class Student_class:

    def __init__(student, first_name, last_name, is_matched, prefs):
        '''creates a student with 5 arguments'''
        student.first_name = first_name
        student.last_name = last_name
        student.is_matched = is_matched
        student.prefs = prefs
