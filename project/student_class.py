class Student_class:

    def __init__(student, name, email, is_matched, prefs):
        '''creates a student with 5 arguments'''
        student.name = name
        student.email = email
        student.is_matched = is_matched
        student.prefs = prefs
